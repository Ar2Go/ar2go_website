# public/brand/ — pendiente

Esta carpeta debe contener los archivos de marca reales:

- `ar2go-logotipo.svg` — logotipo horizontal.
- `ar2go-isotipo.svg` — isotipo cuadrado (favicon y `og:image`).

**No existen todavía.** No se inventa el logo: esta carpeta queda vacía (salvo
este README) hasta que se reciban los SVG definitivos. Ver `CLAUDE.md` §7.

Cuando lleguen los archivos:

1. Colocarlos aquí con esos nombres exactos.
2. Actualizar `app/layout.tsx` (favicon) y los metadatos de `og:image`
   (paso 6 de `PLAN.md`) para que los referencien.
3. Borrar este README o dejar solo la nota de este paso como historial.
